#include "dhtml.hpp"

int main(int argc, char** argv)
{
    //debug lets set it to true for now
    bool Server = true;

    // Entry point
    // Parse Arguments
    // initialize stuff
    // create either a server or a client object
    // call run on the client or server
    // close and delete the object

    return 0;
}